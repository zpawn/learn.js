// @flow

// boolean
// string
// number
// null
// undefined / void
// symbol

function divide (a: number, b: number): string {
    return a / b
}

const s = divide(8, 5);
const ss = divide('foo', [1, 3]);
